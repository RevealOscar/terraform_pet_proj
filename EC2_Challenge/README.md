```
Terraform challenge

1.) Create a DB Server and output the private IP address of the DB Server
2.) Create a Web Server and ensure it has a fixed public IP address
3.) Create a Security Group for the web server opening ports of 80 and 443
4.) Run a script on the web server that installs Apache and creates a simple index.html file

```